import React from 'react';
import ReactDOM from 'react-dom/client';
import "./index.css"
import Movie_trailer from './Movie_trailer';


const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <Movie_trailer />
  </React.StrictMode>
);

